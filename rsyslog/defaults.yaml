# vi: set ft=yaml: #
rsyslog:
  lookup:
    package: rsyslog
    service: rsyslog
    runuser: root
    rungroup: root
    config: /etc/rsyslog.conf
    workdirectory: /var/spool/rsyslog
    custom_config_path: /etc/rsyslog.d
  custom: []
  config: []
